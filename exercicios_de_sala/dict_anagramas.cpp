

unordered_map<string, vector<string>>
construir_dict_anagramas(const vector<string>& palavras){
    for(const string& palavra : palavras){
        string chave = palavra;
        sort(chave.begin(), chave.end());
        
    }
}